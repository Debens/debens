{
  "name": "@training/mobile-atoms",
  "version": "0.0.1",
  "description": "react-native atomic component",
  "license": "MIT",
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc -b",
    "compile": "yarn build --noEmit",
    "clean": "yarn build --clean",
    "test": "jest",
    "test:coverage": "yarn test --coverage",
    "test:ci": "yarn test:coverage --ci --maxWorkers 1",
    "lint": "eslint .",
    "lint:fix": "yarn lint --fix",
    "lint:staged": "lint-staged"
  },
  "dependencies": {
    "styled-components": "^5.3.5"
  },
  "devDependencies": {
    "@babel/core": "^7.17.9",
    "@babel/preset-env": "^7.16.11",
    "@babel/preset-typescript": "^7.16.7",
    "@training/config-typescript": "^0.0.1",
    "@training/eslint-config": "^0.0.1",
    "@training/mobile-testing": "^0.0.1",
    "@types/jest": "^27.4.1",
    "@types/styled-components-react-native": "^5.1.0",
    "@types/styled-system": "^5.1.14",
    "@types/styled-system__should-forward-prop": "^5.1.2",
    "babel-jest": "^28.0.3",
    "jest": "^28.0.3",
    "lint-staged": "^12.4.1",
    "prettier": "^2.6.2",
    "tslib": "^2.4.0",
    "typescript": "^4.6.3"
  }
}
