{
    "name": "@debens/mobile-molecules",
    "version": "0.0.1",
    "description": "react-native atomic component",
    "license": "MIT",
    "files": [
        "build"
    ],
    "scripts": {
        "build": "tsc -b",
        "compile": "yarn build --noEmit",
        "clean": "yarn build --clean",
        "test": "jest --passWithNoTests",
        "test:coverage": "yarn test --coverage",
        "test:ci": "yarn test:coverage --ci --maxWorkers 1",
        "lint": "eslint src",
        "lint:fix": "yarn lint --fix",
        "lint:staged": "lint-staged"
    },
    "dependencies": {
        "@debens/mobile-atoms": "0.0.2-alpha.0"
    },
    "devDependencies": {
        "@babel/core": "^7.17.9",
        "@babel/preset-env": "^7.16.11",
        "@babel/preset-typescript": "^7.16.7",
        "@debens/config-typescript": "0.0.2-alpha.0",
        "@debens/eslint-config": "0.0.2-alpha.0",
        "@debens/mobile-testing": "0.0.2-alpha.0",
        "@types/jest": "^27.4.1",
        "babel-jest": "^28.0.3",
        "jest": "^28.0.3",
        "lint-staged": "^12.4.1",
        "prettier": "^2.6.2",
        "tslib": "^2.4.0",
        "typescript": "^4.6.3"
    }
}
